# Environment variables — never commit API keys
.env

# Local-only docker-compose overrides (e.g. host port remapping)
docker-compose.override.yml

# Image transfer artifacts (built locally, scp'd to a k3s node, then imported)
obs-*.tar

# Local agent / tooling state
.claude/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
eggs/
parts/
var/
sdist/
develop-eggs/
.installed.cfg
lib/
lib64/
wheels/

# Virtual environments
.venv/
venv/
env/
ENV/

# pytest
.pytest_cache/
.coverage
htmlcov/

# mypy
.mypy_cache/

# ruff
.ruff_cache/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Docker volumes / local runtime data (not needed in repo)
prometheus_data/
loki_data/
data/

# IDE
.idea/
.vscode/
*.swp
*.swo
